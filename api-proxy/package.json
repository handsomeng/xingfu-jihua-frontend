{
  "name": "xingfu-api-proxy",
  "version": "0.1.0",
  "description": "幸福计划本地 API 代理：转发 DeepSeek 调用，避免 API key 暴露在前端",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
